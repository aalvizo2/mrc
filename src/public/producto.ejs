<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="img/favicon.png" sizes="150x150" />
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css" integrity="sha384-b6lVK+yci+bfDmaY1u0zE8YYJt0TZxLEAFyYSLHId4xoVvsrQu3INevFKo+Xir8e" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100&family=Quicksand&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <link rel="stylesheet" href="css/footer.css">
    <link rel="stylesheet" href="css/navbar.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <script src="https://kit.fontawesome.com/5210f6d31f.js" crossorigin="anonymous"></script>
    <title>Producto</title>
    <style>
      .card {
        width: 350px;
        height: 450px;
      }
      .card-img-top {
        height: 250px;
        object-fit: cover; /* Mantiene la proporción de la imagen pero corta lo que sobre */
      }
      .card-title {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis; /* Agrega los "..." si el texto es muy largo */
      }
    </style>
    
</head>
<body>
    <%-include('./components/header') %>
     
   <div class="top"></div>
   <div class="container">
      <div class="row pt-5">
        <% for (let i = 0; i < datos.length; i++) { %>
          <div class="col-12 col-md-4 mb-4 d-flex align-items-stretch">
            <div class="card h-100" style="width: 350px;">
              <a href="details?id=<%= datos[i].id %>">
                <img  src="<%= urls[i] %>" class="card-img-top" style="width: 100%; height: 250px; object-fit: cover;" alt="<%= datos[i].nombre_prod %>">
              </a>
              <div class="card-body text-center">
                <h5 class="card-title" style="white-space: nowrap; overflow: hidden; text-overflow: ellipsis;"><%= datos[i].nombre_prod %></h5>
                <p class="card-text"><b>$<%= parseFloat(datos[i].precio_publico).toLocaleString() %>.00</b></p>
              </div>
            </div>
          </div>
          
        <% } %>
        
      </div>
    </div>
    


   <nav aria-label="Page navigation" class="mt-4">
    <ul class="pagination justify-content-center">
      <% if (currentPage > 1) { %>
        <li class="page-item">
          <a class="page-link btn btn-custom mx-1" href="producto">Inicio</a>
        </li>
      <% } %>
      <% for (let i = 1; i <= totalPages; i++) { %>
        <li class="page-item <%= i === currentPage ? 'active' : '' %>">
          <a 
            class="page-link btn <%= i === currentPage ? 'btn-custom text-white' : 'btn-outline-custom' %> mx-1" 
            href="?pagina=<%= i %>"
          >
            <%= i %>
          </a>
        </li>
      <% } %>
    </ul>
  </nav>
  
    
  
  
  
  <%-include('./components/footer')%>
    
    <script src="script/responsive.js"></script>
</body>
</html>   
        