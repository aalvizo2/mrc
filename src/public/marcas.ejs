<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="img/favicon.png" sizes="150x150" />
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css" integrity="sha384-b6lVK+yci+bfDmaY1u0zE8YYJt0TZxLEAFyYSLHId4xoVvsrQu3INevFKo+Xir8e" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100&family=Quicksand&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <link rel="stylesheet" href="css/footer.css">
    <link rel="stylesheet" href="css/navbar.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <script src="https://kit.fontawesome.com/5210f6d31f.js" crossorigin="anonymous"></script>
    <title>Producto</title>
    <style>
        .card-img-top {
            object-fit: cover; /* Asegura que la imagen ocupe toda el área disponible */
            height: 200px; /* Altura uniforme para todas las imágenes */
        }
    
        .btn-custom {
            background-color: #8E0A05;
            color: white;
            border: none;
        }
    
        .btn-custom:hover {
            background-color: #720804;
        }
    
        .pagination .page-link {
            color: #8E0A05;
        }
    
        .pagination .page-item.active .page-link {
            background-color: #8E0A05;
            border-color: #8E0A05;
            color: #ffff;
        }
    </style>
</head>
<body>
    <%- include('./components/header')%>
    
        <div class="top"></div>
        
        <div class="container mt-5">
            <div class="row">
                <% if (Data.length === 0) { %>
                    <div class="d-flex justify-content-center align-items-center vh-100">
                        <h1 class="display-4 text-center text-muted">No hay datos</h1>
                    </div>
                <% } else { %>
                    <% for (let i = 0; i < Data.length; i++) { %>
                        <div class="col-sm-12 col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <img src="<%= urls[i] %>" class="card-img-top" alt="<%= Data[i].nombre_prod %>">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title"><%= Data[i].nombre_prod %></h5>
                                    <p class="card-text mb-4">Precio: $<%= Data[i].precio %></p>
                                    <a href="/details?id=<%= Data[i].id %>" class="btn btn-custom mt-auto">Ver Detalles</a>
                                </div>
                            </div>
                        </div>
                    <% } %>
                <% } %>
            </div>
        
            <!-- Paginación -->
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center mt-4">
                    <!-- Botón "Anterior" -->
                    <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                        <a class="page-link" href="/marca?marca=<%= marca %>&page=<%= currentPage - 1 %>">Anterior</a>
                    </li>
        
                    <!-- Botones de las páginas -->
                    <% for (let i = 1; i <= totalPages; i++) { %>
                        <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                            <a class="page-link" href="/marca?marca=<%= marca %>&page=<%= i %>"><%= i %></a>
                        </li>
                    <% } %>
        
                    <!-- Botón "Siguiente" -->
                    <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                        <a class="page-link" href="/marca?marca=<%= marca %>&page=<%= currentPage + 1 %>">Siguiente</a>
                    </li>
                </ul>
            </nav>
        </div>

        <%-include('./components/footer')%>
        
    <script src="script/responsive.js"></script>
          

</body>
</html>